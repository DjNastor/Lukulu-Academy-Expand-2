import Link from 'next/link';import {Download,Music2,Upload} from 'lucide-react';import {Nav} from '@/components/Nav';
const steps=[[Download,'Download','Get stems and template.','Download','#'],[Music2,'Make','Keep the master below -1 dB.','Practice','/labs/mixer-eq'],[Upload,'Upload','Send audio, ZIP and notes.','Upload','/labs/submit']] as const;
export default function Assignment(){return <><Nav/><main className="task-shell"><header className="page-header"><div className="page-header-copy"><p className="eyebrow">Assignment · Due Friday</p><h1 className="page-title">Mix Practice</h1></div></header><div className="task-grid">{steps.map(([Icon,title,text,action,href])=><article className="card" key={title}><div className="icon"><Icon size={23}/></div><h3>{title}</h3><p>{text}</p><Link className={title==='Make'?'button ghost':'button'} href={href}>{action}</Link></article>)}</div></main></>}
