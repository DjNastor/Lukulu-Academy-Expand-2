import Link from 'next/link';import { Nav } from '@/components/Nav';import { mvpLabs, sampleExercise, gradingModel } from '@/lib/labs';
export default function Labs(){return <><Nav/><main className="shell section"><p className="eyebrow">Lukulu DAW Learning Lab</p><h2>Focused production exercises.</h2><p className="lead">Each lab stores structured project state, runs automatic technical checks, and supports tutor review. Heavy audio analysis runs in a worker service, not ordinary Vercel request functions.</p><div className="grid">{mvpLabs.map((lab,i)=><article className="card" key={lab.slug}><span className="num">0{i+1}</span><h3>{lab.title}</h3><p>{lab.goal}</p><p className="meta">Checks: {lab.checks.join(', ')}</p><Link className="button" href={`/labs/${lab.slug}`}>Open lab</Link></article>)}</div><section className="card" style={{marginTop:24}}><p className="eyebrow">Exercise definition and grading</p><pre style={{whiteSpace:'pre-wrap',color:'#b9d765'}}>{JSON.stringify({sampleExercise,gradingModel},null,2)}</pre></section></main></>}
