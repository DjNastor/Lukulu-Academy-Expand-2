import Link from 'next/link';import {Play} from 'lucide-react';import {Nav} from '@/components/Nav';import {mvpLabs} from '@/lib/labs';const short=(x:string)=>x.replace('Drum Step Sequencer','Drums').replace('Piano Roll and Chord Lab','Chords').replace('Arrangement Timeline','Arrangement').replace('Mixer and EQ Lab','Mixing');export default function Labs(){return <><Nav/><main className="page"><header className="page-header"><div className="page-header-copy"><p className="eyebrow">Learn</p><h1 className="page-title">Pick one skill.</h1><p className="lead">Practice here. Then use your DAW.</p></div></header><div className="card-grid">{mvpLabs.map((lab,i)=><Link className="card course-card" href={`/labs/${lab.slug}`} key={lab.slug}><div className="level"><span>0{i+1}</span><span>Beginner</span></div><h3>{short(lab.title)}</h3><span className="button ghost"><Play size={15}/> Start</span></Link>)}</div></main></>}
