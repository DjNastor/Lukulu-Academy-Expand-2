import Link from 'next/link';import { Nav } from '@/components/Nav';import { mvpLabs } from '@/lib/labs';
export default function Labs(){return <><Nav/><main className="shell section"><p className="eyebrow">Learn</p><h1>Pick one skill.</h1><p className="lead" style={{marginLeft:0}}>Practice in the browser. Then use your real DAW.</p><div className="grid">{mvpLabs.map((lab,i)=><Link className="card big" href={`/labs/${lab.slug}`} key={lab.slug}><span className="num">0{i+1}</span><h3>{lab.title.replace('Drum Step Sequencer','Drums').replace('Piano Roll and Chord Lab','Chords').replace('Arrangement Timeline','Arrangement').replace('Mixer and EQ Lab','Mixing')}</h3><p>{lab.checks.slice(0,2).join(' · ')}</p><span className="button ghost">▶ Start</span></Link>)}</div></main></>}
