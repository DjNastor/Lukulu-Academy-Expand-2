import { Nav } from '@/components/Nav';
const clips=['Intro drums','Bass','Chords','Lead','Vocal','Build','Drop','Breakdown','Outro'];
export default function Page(){return <><Nav/><main className="shell section"><p className="eyebrow">Clip arrangement exercise</p><h2>Arrangement Timeline Lab</h2><p className="lead">Students drag prepared clips onto a timeline. The platform grades clip positions, section lengths and required structure.</p><div className="lab-frame"><div className="transport"><span className="pill">3:00 target</span><span className="pill">8/16 bar sections</span><span className="pill">Loop audition</span></div><div className="timeline">{clips.map((c,i)=><div className="track" key={c}><span style={{width:130}}>{c}</span><i className="clip" style={{marginLeft:`${(i%4)*12}%`,width:`${22+(i%3)*8}%`}}/></div>)}</div></div></main></>}
