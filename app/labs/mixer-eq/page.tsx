import { Nav } from '@/components/Nav';
export default function Page(){return <><Nav/><main className="shell section"><p className="eyebrow">Mixer and EQ concept exercise</p><h2>Mixer and EQ Lab</h2><p className="lead">Balance supplied stems, avoid clipping, pan intentionally and identify frequency ranges with acceptable scoring windows.</p><div className="grid"><div className="card"><h3>Gain staging</h3><p>Keep master peak at or below -1 dB while using at least six channels.</p></div><div className="card"><h3>EQ correction</h3><p>Remove vocal low-end mud without making the vocal thin. Accepted centre: 200–320 Hz.</p></div><div className="card"><h3>Reflection</h3><p>Students explain decisions so they cannot pass by random knob movement.</p></div></div><div className="mixer" style={{marginTop:24}}>{['Kick','Bass','Vocal','Perc','Chord','Master'].map((x,i)=><div key={x}><p className="meta">{x}</p><div className="fader" style={{background:`linear-gradient(to top,var(--gold) ${45+i*6}%,#1a211f ${45+i*6}%)`}}/></div>)}</div></main></>}
